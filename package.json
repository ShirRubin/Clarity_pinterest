{
  "name": "clarity-pinterest",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Clarity Bucket Lists — Pinterest content pipeline (idea → list → design → review → publish → stats)",
  "scripts": {
    "clarity": "tsx src/cli.ts",
    "setup-notion": "tsx scripts/setup-notion.ts",
    "backfill": "tsx scripts/backfill-to-notion.ts",
    "test": "tsx --test tests/schedule.test.ts tests/approve.test.ts"
  },
  "dependencies": {
    "@notionhq/client": "^2.2.15",
    "dotenv": "^16.4.5",
    "playwright-core": "^1.62.1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
